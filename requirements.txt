# HTTP / scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
playwright>=1.44.0          # for JS-rendered pages (Spotahome, etc.)
playwright-stealth>=2.0.0   # reduces bot-detection fingerprinting

# Google Sheets
gspread>=6.1.0
google-auth>=2.29.0

# Config & env
python-dotenv>=1.0.1
pyyaml>=6.0.1

# Utilities
python-dateutil>=2.9.0
